破坏单例模式的方式：
1、实现了Serializable接口的单例，可以通过序列化与反序列化对象的操作进行破坏单例的实例
    可以通过重写readResolve方法返回单例实例，不过jdk底层还是构造了一个新的实例化对象，只是通过readResolve方法替换了实例返回
2、通过jdk的反射机制，设置accessable，进行强制访问构造方法，通过反射的构造方法进行实例化
    可以通过在构造方法中添加是否是已经实例化过的判断，对已经实例化过的操作进行异常或者跳过
3、枚举类从jdk底层屏蔽了序列化的操作，并默认提供了构造方法私有话的机制，在枚举类内部必须实例化好固定的实例